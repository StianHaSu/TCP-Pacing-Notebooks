#!/usr/bin/env bpftrace

kprobe:tcp_enter_loss
{
    $sk = (struct sock *)arg0;
    $tp = (struct tcp_sock *)$sk;
    
    printf("LOSS detected - ssthresh=%u cwnd=%u\n",
           $tp->snd_ssthresh, $tp->snd_cwnd);
}